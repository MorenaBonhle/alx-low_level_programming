#include <stdio.h>

/**
 * main - The function prints the lowercase character
 *
 * Return: (0)
 */

int main(void)
{
int ch;

for (ch = 0; ch <= 9; ch++)
{
putchar(ch);
}

putchar('\n');

/* your code goes there */
return (0);
}
